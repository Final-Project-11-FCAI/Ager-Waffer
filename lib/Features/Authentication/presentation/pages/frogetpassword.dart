import 'package:flutter/material.dart';

class Frogetpassword extends StatelessWidget {
  const Frogetpassword({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Froget Password'),),);
  }
}